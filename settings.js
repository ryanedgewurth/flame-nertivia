// Commands for "Settings" Section
var fs = require('file-system');
var settings 
/* fs.readFile('../configs/settings.json', 'utf8', function(err, contents) {
    settings = JSON.parse(contents);
});
exports.getSetting = function(settingid, user) {
	var settingname = user + "_" + settingid;
	return settings[settingname];
} */
